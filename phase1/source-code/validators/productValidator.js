// Core code

const { body } = require("express-validator");

// Code feature validation (standby)

const create = [(req, res, next) => next()];

const put = [(req, res, next) => next()];

const patch = [(req, res, next) => next()];

module.exports = { create, put, patch };
